#labels Tutorial
= Introduction =

How to update your local files and the cloud files


= Details =
==Pulling Code From Upstream==
How to update your local files

  # right click project folder (default Quasics2015Code)
  # Hover over team and select "Update to HEAD" from menu

==Committing Code==

  # *_ALWAYS_ PULL CODE FROM UPSTREAM FIRST. I REPEAT _ALWAYS_*
  # Right click project folder
  # Hover over team
  # Select commit

*Remember*
We can see who messed up the code if it gets messed up
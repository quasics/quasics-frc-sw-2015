#ifdef ENABLE_OLD_ELEVATOR
#include "Subsystems/ElevatorStage.h"

#include <iostream>

ElevatorStage::ElevatorStage(const char* name) : frc::Subsystem(name) {
}

ElevatorStage::~ElevatorStage() {
}

void ElevatorStage::InitDefaultCommand() {
#ifndef DISABLE_ELEVATOR_DEFAULTS
  SetDefaultCommand(new AdjustElevatorStage(*this));
#else
  std::cerr << "***** WARNING: No default " << this->GetName()
            << " command configured!\n";
#endif  // DISABLE_ELEVATOR_DEFAULTS

#ifdef DISABLE_ELEVATOR_LIMIT_SWITCHES
  std::cerr << "***** WARNING: " << this->GetName()
            << " limit switches will be ignored!\n";
#endif

  // BEGIN AUTOGENERATED CODE, SOURCE=ROBOTBUILDER ID=DEFAULT_COMMAND

  // END AUTOGENERATED CODE, SOURCE=ROBOTBUILDER ID=DEFAULT_COMMAND
}
#endif // ENABLE_OLD_ELEVATOR
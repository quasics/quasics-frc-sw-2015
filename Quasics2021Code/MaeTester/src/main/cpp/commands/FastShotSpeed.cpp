// Copyright (c) FIRST and other WPILib contributors.
// Open Source Software; you can modify and/or share it under the terms of
// the WPILib BSD license file in the root directory of this project.

#include "commands/FastShotSpeed.h"
/*
FastShotSpeed::FastShotSpeed(Shooter* shooter, double power)
    : shooter(shooter), power(power){
  // Use addRequirements() here to declare subsystem dependencies.
  addRequirements(shooter);
}

// Called when the command is initially scheduled.
void FastShotSpeed::Initialize() {
  Shooter->setSpeed(power);
}
// Called repeatedly when this Command is scheduled to run
void FastShotSpeed::Execute() {
  shooter->setSpeed(power); //makes yeet wheel keep spinning
}

// Called once the command ends or is interrupted.
void FastShotSpeed::End(bool interrupted) {
  Shooter->stop();
}

// Returns true when the command should end.
bool FastShotSpeed::IsFinished() {
  return false;
}
*/
